# 文件服务器权限系统改造方案

## 1. 概述

本次改造将为文件上传服务器引入分级权限系统，满足以下需求：
- 管理密钥：可管理所有文件，仅内网可申请
- 临时密钥：用于文件分享，公网可申请，只能访问自有文件，过期后自动删除文件

## 2. 当前架构分析

### 2.1 现有实体关系
```
ApiKey (API密钥表)
  ├─ Id
  ├─ Key
  ├─ Description
  ├─ CreatedAt
  ├─ ExpiresAt
  └─ IsDeleted

FileItem (文件表)
  ├─ Id
  ├─ FileName
  ├─ StoredFileName
  ├─ FileSize
  ├─ ContentType
  └─ UploadedAt
```

### 2.2 现有流程
1. 管理员通过内网 `/api/admin/keys` 创建 API Key
2. 用户使用 Key 访问所有文件操作
3. 无权限隔离，所有 Key 权限相同

## 3. 改造方案

### 3.1 新增枚举：ApiKeyType
```csharp
public enum ApiKeyType
{
    /// <summary>
    /// 管理密钥 - 可管理所有文件
    /// </summary>
    Admin = 1,
    
    /// <summary>
    /// 临时密钥 - 只能访问自有文件，过期后文件自动删除
    /// </summary>
    Temporary = 2
}
```

### 3.2 ApiKey 实体改造
```csharp
public class ApiKey
{
    public int Id { get; set; }
    public string Key { get; set; } = string.Empty;
    public string Description { get; set; } = string.Empty;
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
    public DateTime ExpiresAt { get; set; }
    public bool IsDeleted { get; set; } = false;
    
    // 新增字段
    public ApiKeyType KeyType { get; set; } = ApiKeyType.Admin;
    
    public bool IsValid()
    {
        return !IsDeleted && DateTime.UtcNow < ExpiresAt;
    }
}
```

### 3.3 FileItem 实体改造
```csharp
public class FileItem
{
    public int Id { get; set; }
    public string FileName { get; set; } = string.Empty;
    public string StoredFileName { get; set; } = string.Empty;
    public long FileSize { get; set; }
    public string ContentType { get; set; } = string.Empty;
    public DateTime UploadedAt { get; set; } = DateTime.UtcNow;
    
    // 新增字段：关联上传使用的密钥
    public int? ApiKeyId { get; set; }
    
    // 导航属性
    public ApiKey? ApiKey { get; set; }
}
```

### 3.4 数据库升级（使用SQL脚本）

**生产环境建议使用SQL脚本直接升级，而不是EF Core迁移！**

#### 创建 ApiKeyType 枚举类型
```sql
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'apikeytype') THEN
        CREATE TYPE ApiKeyType AS ENUM ('Admin', 'Temporary');
    END IF;
END$$;
```

#### 为 ApiKeys 表新增 KeyType 字段
```sql
ALTER TABLE "ApiKeys" ADD COLUMN IF NOT EXISTS "KeyType" TEXT DEFAULT 'Admin';
```

#### 为 Files 表新增 ApiKeyId 字段
```sql
ALTER TABLE "Files" ADD COLUMN IF NOT EXISTS "ApiKeyId" INTEGER;
```

#### 添加外键约束
```sql
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM information_schema.table_constraints 
                   WHERE constraint_name = 'FK_Files_ApiKeys_ApiKeyId') THEN
        ALTER TABLE "Files" 
        ADD CONSTRAINT "FK_Files_ApiKeys_ApiKeyId" 
        FOREIGN KEY ("ApiKeyId") REFERENCES "ApiKeys"("Id") ON DELETE SET NULL;
    END IF;
END$$;
```

#### 创建索引提高查询性能
```sql
CREATE INDEX IF NOT EXISTS "IX_Files_ApiKeyId" ON "Files"("ApiKeyId");
```

#### 完整升级脚本（合并版）
把以上SQL合并到一个 `upgrade_v2.sql` 文件中，一次性执行。

## 4. API 接口改造

### 4.1 AdminController 改造

#### 新增：公网申请临时密钥接口
```
POST /api/public/keys
Query Parameters:
  - description: 密钥描述
  - expireMinutes: 过期时间（分钟），默认 60

Response:
  {
    "id": 1,
    "key": "abc123...",
    "keyType": "Temporary",
    "expiresAt": "2024-04-15T10:30:00"
  }
```

#### 改造：创建密钥接口（保留内网）
```
POST /api/admin/keys
Query Parameters:
  - description: 密钥描述
  - expireMinutes: 过期时间（分钟），默认 1440 (24小时)
  - keyType: 密钥类型 (Admin/Temporary)，默认 Admin
```

### 4.2 FileApiController 改造

所有文件操作需要根据密钥类型进行权限过滤：

#### 权限规则
| 操作 | Admin Key | Temporary Key |
|------|-----------|---------------|
| GET /api/files | 返回所有文件 | 仅返回 ApiKeyId = 当前密钥的文件 |
| GET /api/files/{id} | 可访问所有文件 | 仅可访问自有文件 |
| POST /api/files | 可上传 | 可上传，自动关联 ApiKeyId |
| DELETE /api/files/{id} | 可删除所有文件 | 仅可删除自有文件 |
| GET /api/files/download/{id} | 可下载所有文件 | 仅可下载自有文件 |

## 5. 认证中间件改造

### 5.1 上下文传递
中间件验证通过后，将当前密钥信息存入 `HttpContext.Items` 供后续使用：
```csharp
context.Items["CurrentApiKey"] = apiKey;
```

### 5.2 新增服务：IPermissionService
```csharp
public interface IPermissionService
{
    /// <summary>
    /// 检查是否可以访问指定文件
    /// </summary>
    Task<bool> CanAccessFileAsync(int fileId, ApiKey currentKey);
    
    /// <summary>
    /// 获取当前密钥可访问的文件查询
    /// </summary>
    IQueryable<FileItem> GetAccessibleFilesQuery(ApiKey currentKey, IQueryable<FileItem> allFiles);
}
```

## 6. 后台清理服务

### 6.1 新增：BackgroundCleanupService
托管后台服务，定期（如每小时）清理：
1. 已过期且类型为 Temporary 的密钥
2. 关联这些密钥的文件记录
3. 对应的物理文件

### 6.2 清理逻辑
```
FOR EACH expired Temporary ApiKey:
    ↓
GET ALL FileItem WHERE ApiKeyId = key.Id
    ↓
DELETE physical files from wwwroot/uploads
    ↓
DELETE FileItem records
    ↓
DELETE ApiKey record
```

## 7. 接口详细设计

### 7.1 公钥申请接口 (新增)
```
POST /api/public/keys

Request:
  Query:
    description=temp-share
    expireMinutes=30

Response (201 Created):
  {
    "id": 5,
    "key": "a1b2c3d4e5f6...",
    "description": "temp-share",
    "keyType": "Temporary",
    "createdAt": "2024-04-15T08:00:00",
    "expiresAt": "2024-04-15T08:30:00"
  }
```

### 7.2 文件列表接口 (改造)
```
GET /api/files?key=xxx

Admin Key Response:
  [
    { "id": 1, "fileName": "doc.pdf", ..., "apiKeyId": 1 },
    { "id": 2, "fileName": "image.jpg", ..., "apiKeyId": 3 }
  ]

Temporary Key Response (仅自有文件):
  [
    { "id": 3, "fileName": "share.zip", ..., "apiKeyId": 5 }
  ]
```

### 7.3 文件上传接口 (改造)
```
POST /api/files?key=xxx
Content-Type: multipart/form-data

无论 Key 类型，上传的文件会自动关联到当前 Key
```

## 8. 安全考虑

1. **临时密钥限制**：
   - 最大过期时间：建议不超过 24 小时
   - 单次文件大小限制：可配置
   - 总文件大小限制：可配置

2. **内网接口保护**：
   - 管理接口继续使用 localhost 限制
   - 可考虑增加内网 IP 白名单

3. **速率限制**：
   - 公钥申请接口建议增加速率限制

## 9. 实施步骤

1. **Step 1**: 创建新的实体属性和枚举
2. **Step 2**: 准备并执行SQL数据库升级脚本（在生产环境数据库执行）
3. **Step 3**: 实现权限服务
4. **Step 4**: 改造认证中间件
5. **Step 5**: 改造现有 API 控制器
6. **Step 6**: 新增公钥申请接口
7. **Step 7**: 实现后台清理服务
8. **Step 8**: 更新 Swagger 文档
9. **Step 9**: 编写单元测试
10. **Step 10**: 部署测试

## 10. 兼容性说明

- 现有 Admin Key 将自动保留，无需重新生成
- 现有文件的 ApiKeyId 为 NULL，Admin Key 仍可访问
- 可以编写数据迁移脚本为历史文件关联默认 Admin Key（可选）
